Run the shell command `lens status` and show me the full output.

Include the index health, token economy, and last queries in your response.
If lens is not found, tell me to run: pip install "context-lens-v2[parse]" && lens index
